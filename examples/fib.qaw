// fib.qaw — 斐波那契数列

func fib(n: int) -> int {
    if n < 2 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

func main() {
    for i from 0 to 9 {
        let r = fib(i);
        print("fib(${i}) = ${r}");
    }
}