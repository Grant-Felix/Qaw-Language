// 示例：结构体和枚举
package shapes

struct Point {
    x: f64,
    y: f64,
}

enum Color {
    Red,
    Green,
    Blue,
}

func new_point(x: f64, y: f64) -> Point {
    return Point { x: x, y: y }
}

func main() -> void {
    let p = new_point(1.0, 2.0)
    print("点已创建")
}