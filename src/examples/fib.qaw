// 文件名: fib.qaw
// 演示递归函数（斐波那契）

func fib(n: int) -> int {
    if n < 2 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

func main() -> void {
    print("斐波那契数列:")
    for i from 0 to 10 {
        print("fib(${i}) = ${fib(i)}")
    }
}
