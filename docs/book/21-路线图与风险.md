# 路线图与风险

[← 返回目录](../1-妖文编程语言从入门到精通.md)

---

## 21.1 版本方案

格式：**`v{大版本}.{小版本}{dev/beta?}`**

| 字段 | 含义 | 范围 |
|:---|:---|:---|
| 大版本 | 正式大版本（破坏性变更 / 全新架构） | 0, 1, 2, ...，无上限 |
| 小版本 | 变动、更新、修复的**最小不可分割独立改变** | 0, 1, 2, ...，无上限 |
| `dev` / `beta` | 开发或测试中的预发布标签（可选） | 仅在主分支不稳定时使用 |

**跳号规则（严格）**：

- **小版本必须递增 +1**——不允许 0.1 → 0.5 跳号
- **大版本升 +1 时，小版本归零**——这是唯一允许的"看似跳号"
- 任何"为了凑里程碑而跳号"都是错误的，反之亦然（不能用未完成的版本号代表将来要做的事）

**版本号含义 = 已完成的小版本计数**，不是"我们要做 X 个版本的承诺"。

**不使用阶段标签（如 POC/MVP/Beta/LTS/RC）作为版本分类**。阶段是营销术语，不是工程概念。任何"阶段"都隐含了"还要再跨一个版本才是真正的版本"的误导。路线图只表达**任务清单**和**里程碑达成条件**。

## 21.1a 分支治理

按 Qaw 项目成熟度，仓库分支结构分阶段：

### 阶段 A：v1.0 完成前（当前）

仓库分为 **2 个常驻分支**：

| 分支 | 用途 |
|:---|:---|
| `master`（开发分支） | 日常开发主线。所有"最小独立改变"先合入这里，触发 CI |
| `testing`（测试分支） | 接收阶段性"接近稳定"的快照，进行功能性、稳定性、安全性、性能测试 |

流转：
```
master → (快照切到) → testing
```
- `testing` 分支在 v1.0 临近完成时启用
- `testing` 上发现的问题 → 修复在 `master` → 重新切快照到 `testing`
- `testing` 通过 → 切到稳定分支

### 阶段 B：稳定版本发布后（v1.0 及之后）

仓库分为 **3 个常驻分支**：

| 分支 | 用途 |
|:---|:---|
| `master`（开发分支） | v1.1 / v1.2 / ... 新功能开发，规则同上 |
| `testing`（测试分支） | 当前开发版本的快照测试，与阶段 A 相同 |
| `stable`（稳定分支） | 最新已发布稳定版本；仅修紧急 bug；每个 LTS 节点打 tag |

流转：
```
master → testing → stable
                ↑      ↓
                └─ 修复回流
```

**LTS 节奏**：稳定分支每年 4 月与 10 月打 LTS tag，承诺 5 年支持期。

### 分支治理原则

1. **`master` 永远可编译、测试通过**——CI 阻断不合格提交
2. **`testing` 接收 `master` 的快照，不是自动同步**——人为判断"够稳定"才切
3. **`stable` 仅接受来自 `testing` 的合并**——紧急修复也走 `testing` → `stable`
4. **所有合并走 PR**——直接 push 不允许
5. **每个 LTS tag 有独立的 CHANGELOG 段落**——可在 `stable` 历史中找到

## 21.2 里程碑（达成条件，非时间节点）

每个里程碑是"当以下条件全部成立时达到"。里程碑之间是若干小版本累积，没有预设数量。

| 里程碑 | 达成条件 | 验证方法 |
|:---|:---|:---|
| **v0.50**（参考） | **完整独立的开发语言**：可写不依赖任何外部 C 库的完整程序 | `bootstrap/` 目录下的最小自包含 demo |
| **v0.100**（参考） | **主流编程语言的全部能力**：泛型/trait/异步/FFI/宏等 | 与 Rust/Go 特性矩阵对照 |
| **v0.150**（参考） | **自举**：用 Qaw 写 Qaw 编译器，产物与 Rust 版字节级一致 | 双编译 diff |
| **v0.200**（参考） | **完全替代主流语言**：性能、工具链、DX 不输 Rust/Go/Java | 第三方 benchmark + 用户调研 |
| **v0.260**（参考） | **基础生态**：移植、独立开发、互操作调用其他语言生态 | 至少 N 个生态项目 |
| **v1.0**（确定） | **除生态外成熟稳定的 Qaw**：功能完整性 + 稳定性 + 安全性 + 性能 + 工具链 + 文档，全部达到"可替代目标领域所有其他语言"的水准。**唯一允许的缺失项是生态**——生态在 v1.0 之后继续演进（v1.0.x / v1.1 / ...）。 | 第三方独立评估：Qaw 在所有目标领域（系统/后端/前端/AI/游戏/运维）的功能/性能/工具链对比主流语言均不落下风；至少 1 个完整项目使用纯 Qaw 实现 ≥ 6 个月生产部署 |

> 上述数字是参考值。具体达成时的版本号取决于实际完成任务数。  
> 例：如果 v0.50 的任务清单包含 50 个最小独立改变，每完成 1 个 +1，到全部完成时自然落在 v0.50。

## 21.3 任务清单驱动的工作模式

**核心规则**：本路线图不预先为每个版本号指定任务。任务完成时，版本号自然递增。

### 当前状态（实时快照）

- **当前小版本**：v0.10
- **下一动作**：v0.11 = 阶段 A 第 1 项（Sound null safety，详见 [docs/v0.10-to-v0.50-规划.md](../../v0.10-to-v0.50-规划.md)）
- **里程碑进度**：v0.50 → 2% (1/64 任务完成；新规划含 64 项，原 50 项已合并）

### 当前阶段任务清单（目标：v0.50 = 完整独立语言）

**完整 64 项任务清单见 [docs/v0.10-to-v0.50-规划.md](../../v0.10-to-v0.50-规划.md)**。该规划基于 16 种主流语言的特性研究（C/C++/Rust/汇编/Java/Go/C#/Python/Scala/Ruby/JS/TS/Swift/Kotlin/Dart/R/Lua/GDScript），按 5 个阶段组织：

| 阶段 | 任务数 | 范围 |
|:---|:---:|:---|
| A：基础类型与安全 | 11 | null safety / 值类型 / Option/Result / Block / defer / with |
| B：标准库与模块 | 15 | core/alloc/collections + IO + qawpm + 模块系统 |
| C：内存与并发 | 12 | @safe 借用检查 + 智能指针 + 协程/通道/select + async |
| D：编译与工具链 | 12 | LLVM 后端 + 泛型 + trait + fmt/doc/test + LSP + clippy |
| E：v0.50 集成 | 14 | 自包含 demo + benchmark + 文档冻结 + v0.50 发布 |

**进度**：1/64 已完成（v0.1 解释器 + 6 示例）。
| Q50：v0.50 发布 | 公开 v0.50；社区反馈；触发 v0.51+ 工作 | ⬜ |

> **达成 v0.50 的标志**：以上 50 个任务全部 ✅。
> 当前 1/50 完成。

### 主流能力阶段任务清单（目标：v0.100）

| 任务类别 | 说明 |
|:---|:---|
| 泛型与单态化 | 函数泛型、结构体泛型、trait bound 推导、单态化代码生成 |
| Trait 系统 | trait 定义、impl、实现约束、对象安全、dyn trait、关联类型 |
| 生命周期 | 显式生命周期参数、生命周期推导、省略规则、HRTB |
| 完整模式匹配 | 字面量/范围/绑定/守卫/`@` 绑定；穷尽性检查 |
| 错误处理 | `Result`/`Option`、`?`、`try` 块、自定义错误 + `From` |
| 完整异步 | `async`/`await`、async runtime（epoll/kqueue/IOCP） |
| 并发原语 | GMP 调度器、协程栈、通道、`select`、Mutex/RwLock |
| FFI | `@extern "C"` 块、`@repr(C)`、类型映射、字符串转换、函数指针 |
| 宏 | `macro_rules!`、内建宏、卫生、过程宏（推迟到 v1.0） |
| 属性与文档 | 内建属性、Markdown 文档、文档测试 |

> **达成 v0.100 的标志**：上述类别全部有可用实现 + 至少 100 个端到端示例覆盖。

### 自举阶段任务清单（目标：v0.150）

| 任务 | 验收标准 |
|:---|:---|
| 用 Qaw 重写 lexer | 与 Rust 版输出字节级一致 |
| 用 Qaw 重写 parser | 同上 |
| 用 Qaw 重写 ast | 同上 |
| 用 Qaw 重写 value | 同上 |
| 用 Qaw 重写 env | 同上 |
| 用 Qaw 重写 interpreter | 同上 |
| 用 Qaw 重写主入口 | 同上 |
| 双编译一致性测试 | Rust 版编译自身源码 与 Qaw 版编译自身源码 字节级 diff 完全一致 |
| 引导版退役 | Rust 版退出生产路径；保留为参考实现 |

### 替代阶段任务清单（目标：v0.200）

| 任务 | 验收标准 |
|:---|:---|
| 性能优化 | 各场景 benchmark ≥ Rust 同场景 80% |
| 完整 LSP | 重命名、查找引用、代码补全、跳转实现 |
| 完整 clippy | ≥ 50 条 lint；社区贡献通道 |
| 完整 profiler | 火焰图 + 调用图 + 内存分析 |
| 完整 debugger | 条件断点 + watchpoint + 多线程调试 |
| 完整测试框架 | 参数化测试 + mock + property-based testing |
| 完整文档生成 | 多 crate 文档 + 全文搜索 + 教程集成 |
| 第三方大型项目移植 | 至少 1 个 10k+ 行开源项目移植成功 |
| 性能对标报告 | 公开的对比 Rust/Go 的 benchmark 报告 |
| 社区规模 | 至少 1000 个 GitHub stars + 100 个外部贡献者 |

### 生态阶段任务清单（目标：v0.260）

| 任务 | 验收标准 |
|:---|:---|
| 互操作扩展 | C/C++/Rust/Go/Python/Java 互操作完整方案 |
| 包生态 | 公共 registry ≥ 1000 个包；包分类清晰 |
| 编辑器生态 | VSCode/Vim/Emacs/IntelliJ 插件成熟 |
| 工具链生态 | 第三方工具（linter/formatter/profiler）≥ 10 个 |
| 教育生态 | 教程、课程、书籍 ≥ 5 套 |
| 企业采用 | 至少 3 家企业在生产环境使用 Qaw ≥ 6 个月 |
| 标准合规 | 与 WebAssembly/GCC/LLVM 等标准生态对接 |

### 超越阶段任务清单（目标：v1.0）

| 任务 | 验收标准 |
|:---|:---|
| 多技术栈单一语言 demo | 一个完整项目（前端+后端+CLI+嵌入式+Web）用纯 Qaw 实现并生产部署 ≥ 6 个月 |
| 性能全面领先 | 与 Rust/Go/C++ 同场景 benchmark 全胜或并列 |
| DX 评估 | 独立第三方调研：Qaw DX 评分 ≥ 主流语言 |
| 生态完备 | 任何常见应用场景有 ≥ 3 个可选 Qaw 包 |
| 长期支持承诺 | 稳定分支 LTS tag 节奏正式发布；至少 5 年支持周期 |
| 社区自治 | 独立基金会/组织接管治理 |

## 21.4 工作模式（计划与变化）

**计划（A 模式）**：
- 每季度初，整理当前阶段任务清单
- 标注优先级、依赖关系、预估工时
- 发布到 docs/book/21 + docs/3

**变化（C 模式）**：
- 任务实际完成时，**版本号自然递增**
- 任务新增/删除/重排：直接在文档更新，不需"批准"
- 里程碑达成条件变更：需在路线图变更日志中说明原因
- 重大方向调整：需 RFC 流程（见 docs/rfcs/）

**变更日志**：
本节按时间倒序记录路线图本身的修改。

| 日期 | 变更 | 原因 |
|:---|:---|:---|
| 2026-06-30 | 从时间节点驱动改为任务清单驱动；删除 v0.5 MVP 里程碑；引入 v0.50/v0.100/v0.150/v0.200/v0.260/v1.0 里程碑 | 用户规则："小版本根据项目小变更计数" + "每个大小版本号必须有明确目的和切实可行的计划" |
| 2026-06-30 | v1.0 定义精化：除生态外成熟稳定，可替代目标领域所有其他语言；生态在 v1.0 后继续演进 | 用户规则："v1.0是除了生态以外，成熟稳定的Qaw" |
| 2026-06-30 | 分支治理章节：v1.0 完成前分 2 分支（master/testing），稳定版发布后分 3 分支（master/testing/stable） | 用户规则："v1.0完成前且即将完成时开始项目分为两个分支" + "稳定版本推出后项目分为三个分支" |
| 2026-06-30 | 路线图禁用阶段标签（POC/MVP/Beta/LTS/RC）；路线图只表达任务清单与里程碑达成条件 | 用户规则："不要MPV、POC这类定义，只要需要做什么然后依次进行" |

## 21.5 风险与备选

### 高风险

| 风险 | 影响 | 缓解 |
|:---|:---|:---|
| 任务量超预期 | 里程碑延期 | 路线图变更日志透明记录；接受延期比妥协质量重要 |
| LLVM/Cranelift 集成复杂度 | 编译器后端延期 | 先评估 Cranelift（更简单），必要时回退到纯 C 后端 |
| 借用检查器正确性证明 | 用户信任受损 | 形式化规范 + 大量 property test + Miri 验证 |
| 自举字节级一致性 | 自举可信度问题 | 严格接受"比特位完全一致"，不妥协 |

### 中风险

| 风险 | 缓解 |
|:---|:---|
| 性能优化困难 | 接受 v0.200 性能 < Rust 80% 的过渡期；持续优化 |
| 生态冷启动 | 提供 FFI + 迁移工具；先做 1-2 个杀手级 demo |
| 社区冷启动 | 主动招募；与高校合作课程；提供商业支持 |
| 文档规模爆炸 | 文档分层（教程/API/原理）；自动生成站 |

### 低风险

| 风险 | 缓解 |
|:---|:---|
| CI 成本 | GitHub Actions + 自托管 runner |
| 商标/域名 | 标准注册流程 |

## 21.6 回退方案

| 场景 | 回退方案 |
|:---|:---|
| LLVM 集成不可行 | 锁定 Cranelift 主版本；或纯 C 后端成熟版本 |
| 自举一致性无法达到 | 保留 Rust 引导版为参考实现；Qaw 版标"自举候选" |
| 标准库功能不够 | 拆分为 std + std-contrib；通过 FFI 依赖成熟库 |
| 性能瓶颈 | 局部模块允许手写汇编；接受"特定场景用 C ABI" |
| 社区停滞 | 调整目标用户群；降低 v1.0 标准 |

## 21.7 路线图治理原则

1. **任务粒度**："最小不可分割独立改变"——每个任务必须有明确可验证的产出
2. **版本自动**：不预先指定"v0.X 做 Y"，让版本号自然反映完成进度
3. **里程碑刚性**：达成条件不打折（v0.50 不允许"差不多完整独立"）
4. **变更透明**：所有路线图本身修改必须记录在变更日志
5. **下游兼容**：v0.X 之间承诺源代码兼容（仅 deprecate，不删除）

---

[← 返回目录](../1-妖文编程语言从入门到精通.md)